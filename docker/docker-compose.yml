version: "3.9"

services:

  qdrant:
    image: qdrant/qdrant:latest
    container_name: medrag_qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: medrag_ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_models:/root/.ollama
  #   environment:
  #     - OLLAMA_HOST=0.0.0.0
  #   restart: unless-stopped

volumes:
  qdrant_data:
  # ollama_models: